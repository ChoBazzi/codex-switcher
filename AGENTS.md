# Repository instructions

## Source of truth

- 작업 전에 `README.md`와 관련 ADR을 읽는다.
- 확정된 설계를 변경할 때는 코드와 관련 ADR을 함께 갱신한다.
- 중요한 기술 결정은 `docs/adr/`에 ADR로 기록한다.
- 로컬 전용 계획 및 메모 파일을 저장소에 커밋하지 않는다.

## Scope

- V1은 macOS와 Codex 계정 2개만 지원한다.
- 요청 범위를 벗어난 Claude, Gemini, 원격 서버 및 다중 사용자 기능을 추가하지 않는다.
- 실패한 요청은 안전성·응답 시작 여부와 무관하게 같은 계정이나 다른 계정으로 자동 재전송하지 않는다.
- 다음 세션의 Wiki 인계는 사용자 입력을 기다리며 전환 준비만으로 모델 요청을 시작하지 않는다.

## Security

- OAuth 토큰, refresh token, Authorization 헤더 및 실제 계정 식별자를 저장소나 로그에 기록하지 않는다.
- 프록시 및 제어 API는 기본적으로 loopback에만 바인딩한다.
- 테스트 fixture에는 실제 요청, 응답 및 토큰 대신 합성 데이터를 사용한다.
- 계정 A의 continuation 식별자를 계정 B에 전달하지 않는다.

## Change discipline

- 현재 요청에 필요한 최소 범위만 변경한다.
- 새 의존성을 추가할 때 목적과 대안을 기록한다.
- 동작 변경에는 대응하는 테스트 또는 재현 가능한 검증 절차를 추가한다.
- 사용자 소유의 기존 변경을 되돌리거나 덮어쓰지 않는다.
